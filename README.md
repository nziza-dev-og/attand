# AttendEase

This is an attendance tracking platform built with Next.js and Firebase.

To get started, take a look at src/app/page.tsx.

**Note:** You will need to configure your Firebase project credentials in `src/lib/firebase.ts`.
